---
:version: 4
:datadir: data
:hierarchy:
  - name: 'YAML hieradata'
    backend: yaml
    paths:
      - "nodes/%{::trusted.certname}"
      - "env/%{::environment}"
      - "domains/%{::domain}"
      - "role/%{::serverrole}"
      - global
